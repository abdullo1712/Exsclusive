// This component is now replaced by the Wishlist page's inline rendering
// Kept for backward compatibility
import React from "react";
import Cards1 from "../cardshome1/Cards1";

function Wishlist_box2() {
  return <Cards1 limit={4} />;
}

export default Wishlist_box2;
